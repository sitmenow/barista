import api from '../../api';
import { actions } from './reducer'
import { actions as globalActions} from '../../reducer';

export const loadBranch = () =>
  async (dispatch: Function, getState: Function) => {
  }

export const loadBarista = () =>
  async (dispatch: Function, getState: Function) => {
  }
